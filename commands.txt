

Git clone

Git and Github

alternatives : AWS, bitbucket, gitlab

git add . >>> add the file to a staging alternatives (file is being tracked by git)

git commit -m "message"  >>> to commit the changes in the tracked file

git push origin main >>> pushing the file to the main branch in Githb repository

git add filename  >>> specified aayittolla file track cheyyan                                          (q >> type cheyyan pattathappol)